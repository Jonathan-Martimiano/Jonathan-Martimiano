name: gitartwork from a contribution graph
 on: 
   push:
   schedule:
     - cron: '* */24 * * *'
 jobs:
   build:
     name: Make gitartwork SVG
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3
       - uses: Jonathan-Martimiano/gitartwork@v1
         with:
            # Use this username's contribution graph  
            user_name: Jonathan-Martimiano
            # Text on contribution graph 
            text: Jonathan Martimiano
       - uses: Jonathan-Martimiano/simple-push-action@v1
